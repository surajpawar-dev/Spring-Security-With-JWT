package com.suraj.authservice.entity;

/**
 * Enum representing the possible roles in the system.
 */
public enum Role {
    ROLE_USER,
    ROLE_MODERATOR,
    ROLE_SUPERVISOR,
    ROLE_MANAGER,
    ROLE_ADMIN
}